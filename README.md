# Harga-Rumah-Jabodetabek

[untitled19.py](https://github.com/user-attachments/files/24370895/untitled19.py)
# -*- coding: utf-8 -*-
"""Untitled19.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1pfJnbjSkv4QbMzJzlRbgeXZb5hR2hvjL
"""

import pandas as pd
import matplotlib.pyplot as plt
from sklearn.preprocessing import StandardScaler
from sklearn.preprocessing import MinMaxScaler
from sklearn.model_selection import train_test_split
from sklearn.linear_model import LinearRegression
from google.colab import files

data = pd.read_csv('jabodetabek_house_price.csv.zip')
data.head()

data.shape

data.info()

data.isnull()

data.isnull().sum().sum()

data['url'].isnull().any()
data['price_in_rp'].isnull().any()
data['title'].isnull().any()
data['address'].isnull().any()
data['district'].isnull().any()
data['city'].isnull().any()
data['lat'].isnull().any()
data['long'].isnull().any()
data['facilities'].isnull().any()
data['property_type'].isnull().any()
data['ads_id'].isnull().any()
data['bedrooms'].isnull().any()
data['bathrooms'].isnull().any()
data['land_size_m2'].isnull().any()
data['building_size_m2'].isnull().any()
data['carports'].isnull().any()
data['certificate'].isnull().any()
data['electricity'].isnull().any()
data['maid_bedrooms'].isnull().any()
data['maid_bathrooms'].isnull().any()
data['floors'].isnull().any()
data['building_age'].isnull().any()
data['year_built'].isnull().any()
data['property_condition'].isnull().any()
data['building_orientation'].isnull().any()
data['garages'].isnull().any()
data['furnishing'].isnull().any()

data.isnull().sum()

median_building_age = data['building_age'].median()
data['building_age'].fillna(median_building_age, inplace=True)
median_year_built = data['year_built'].median()
data['year_built'].fillna(median_year_built, inplace=True)
median_building_size_m2 = data['building_size_m2'].median()
data['building_size_m2'].fillna(median_building_size_m2, inplace=True)

data_filled = data.fillna(0)
data['property_condition'].fillna('Tidak Diketahui', inplace=True)
data['building_orientation'].fillna('Tidak Diketahui', inplace=True)

data_encoded = pd.get_dummies(data, columns=['property_type', 'property_condition', 'building_orientation'])
print(data_encoded)

data_numeric = data.select_dtypes(include=['number'])
print(data_numeric)

scaler = StandardScaler()
data_scaled = scaler.fit_transform(data_numeric)
print(data_scaled)

data.isnull().sum()

X = [col for col in data_encoded.columns if 'property_type_' in col] + ['building_size_m2']
y = ['price_in_rp']
df_encoded = pd.get_dummies(data_encoded, columns=['building_size_m2'], drop_first=True)
print(df_encoded)

X_train, X_test, y_train, y_test = train_test_split(data_encoded[X], data_encoded[y], test_size=0.2, random_state=42)
print(f'Untuk pelatihan (X), {X_train.shape}')
print(f'Untuk pengujian (X), {X_test.shape}')

model = LinearRegression()
model.fit(X_train, y_train)

plt.scatter(X_train['building_size_m2'], y_train)
plt.xlabel('building_size_m2')
plt.ylabel('price_in_rp')

y_prediksi = model.predict(X_test)
print(y_prediksi)

files.download('/content/jabodetabek_house_price.csv.zip')
